extends ../_layout
include ../_mixins/_header
include ../_mixins/_item

block header
  +header

block content 
  .content(style="display: flex; flex-wrap: wrap;")
    for item in items
      +item(item.title, item.img, item.desc)
    
  